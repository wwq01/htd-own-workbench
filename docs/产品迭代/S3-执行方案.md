# V2.0 S3 阶段执行方案 · Tauri 桌面壳

> 状态：**S3-0 前置契约 ✅；S3-1 骨架 ✅ 编译+打包验证通过（CI run 34704536841 全绿，产出 NSIS 安装包 96MB artifact）；S3-2 系统集成 ✅ 已实现（托盘/关闭到托盘/全局快捷键 Ctrl+Alt+H/通知），编译验证走 CI；门禁 22 + 46 = 68 项全绿；S3-3 视觉衔接待做；代码签名暂缓**
> 验证通道：**本机 GNU 编译通道已放弃**（GitHub release 域名被限流，MinGW 续传 40 次 0 字节）；一律走 `git push` → GitHub Actions（`windows-latest` + MSVC，约 8 分钟出结论 + 产出安装包）。本机 Rust 工具链已装但不是必需。
> 定位：S3 是 V2.0 重构的最后一阶段，目标是把已有的 Node 后端 + Vue 前端**原样装进原生壳**，业务代码 0 改动。

---

## 1. 前置状态核对（查代码，不查文档状态栏）

按路线图，S3 的开工条件是 S1、S2 均完成。2026-09-12 逐项实测核对：

| 阶段 | 结论 | 依据 |
|---|---|---|
| S1-1 registry 单一数据源 | ✅ | `frontend/js/registry.js` + `verify-registry.js` 21 项断言 |
| S1-3 状态机收敛 | ✅ 2026-09-12 补齐 | 9 域 / 11 处接入 `resolveStateMachine`；`grep "ALLOWED_TRANSITIONS: [A-Z_]*TRANSITIONS"` 归零 |
| S1-4 / S1-5 / S1-6 / S1-7 | ✅ | 文档状态栏原为空，实测均已实现（状态栏漏勾） |
| S2-1 ~ S2-6 | ✅ | Vite 构建化、图表/图谱组件化、Vault 深化、Note 双轨、RFP 应答、地基组件 |
| **S3-0 sidecar 契约（新增前置）** | ✅ 2026-09-12 | 见 §2 |

---

## 2. S3-0 · Node sidecar 启动契约（不依赖 Rust，已完成）

### 2.1 为什么需要先定契约

Tauri 以 sidecar 方式拉起 Node 后端时，壳与子进程之间必须就**端口、就绪时机、数据目录**达成一致。这部分纯 Node 侧，先落地即可与后续 Rust 侧解耦并行。

### 2.2 契约内容

**环境变量**

| 变量 | 作用 | 默认值 |
|---|---|---|
| `HTD_DESKTOP` | `1`/`true` 进入桌面模式 | 未设置 = 普通模式 |
| `HTD_PORT` | 监听端口 | 17388（**仅允许 17388/17389/17390**） |
| `HTD_DATA_ROOT` | 数据根目录（壳传 AppData 路径） | `D:\荒天帝工作台` 或 `settings.json` 的 `dataRoot` |

**桌面模式与普通模式的三处差异**

1. **不自动打开系统浏览器** —— 页面由壳内 webview 加载。
2. **跳过「同端口已有实例则复用并退出」分支** —— 唯一实例由壳的 single-instance 保证。若 sidecar 在此 `exit(0)`，壳会判定子进程异常终止，反而拿不到端口。
3. **就绪后输出机器可读行**：

```
HTD_READY {"port":17388,"host":"127.0.0.1","url":"http://127.0.0.1:17388","apiPrefix":"/api/v1","dataRoot":"..."}
```

壳以 `/^HTD_READY (.+)$/` 逐行匹配 stdout，解析出 `port` 后令 webview 加载 `url`。

### 2.3 顺带修掉的真实缺陷：端口顺延导致写入全被 403

`originGuard` 的白名单读的是 `appConfig.port`。当 17388 被占用、服务顺延到 17389 时，浏览器实际 Origin 是 `http://127.0.0.1:17389`，而白名单仍是 17388 —— **所有 POST/PUT/PATCH/DELETE 一律 403**，且现象极具迷惑性（页面能开、列表能查，一保存就失败）。

修复：`server.js` 在 `listen` 成功后回写 `appConfig.port = port`，白名单自动跟随实际端口。测试用双向断言锁死（未回写 → 403；回写后 → 非 403）。

### 2.4 端口必须为固定三端口的原因

`findAvailablePort` 只在 17388 / 17389 / 17390 中探测，三者全占用即拒绝启动（不向后续端口顺延）。**S3 不能改用 OS 随机端口**：随机端口会同时破坏 originGuard 白名单与 pkg 打包的一致性约定。壳侧因此必须接受「用三个固定端口之一，全占用则提示用户」的行为。

---

## 3. S3-1 · Tauri + Node sidecar 打包（消费 dist/）

### 3.1 环境要求（**Rust 工具链已具备，GNU 编译前置已补齐**）

- ✅ Rust 工具链：`rustup` + 稳定版 Rust **1.98.1（x86_64-pc-windows-gnu）**，2026-09-12 装好
- ✅ WebView2 Runtime：v152.0.4191.66（已内置，Tauri v2 渲染引擎就绪）
- ✅ MinGW-w64（**GNU target 编译资源硬需求**）：winlibs GCC16/MSVCRT 7z 已下载到
  `$USERPROFILE/.workbuddy/mingw64.7z`（~105MB），提供 `windres`/`gcc`/`ar`/`ld`。
  本机无 VS / MSVC / Windows SDK，故走 GNU target（cargo 编译期嵌 Windows 资源必须 MinGW）
- ✅ Node 24 LTS（已有）
- ✅ crates.io 国内镜像：已写 `~/.cargo/config.toml` 指向 rsproxy.cn，依赖下载走国内加速
- ⏳ 首次 Tauri 编译（含依赖编译）通常 **30–60 分钟**；本会话因 shell 工具瞬时不可用 +
  链路偏慢，真机 `cargo build` 留待工具恢复后一键执行（见 §3.6）

### 3.2 骨架结构（**已建**，待 Rust 就位后编译）

```
src-tauri/
  Cargo.toml               # tauri 2.11.5 / tauri-plugin-shell 2.3.6 / single-instance 2.4.4
  tauri.conf.json          # externalBin: ["binaries/htd-backend"]；窗口初始 about:blank + visible:false
  build.rs                 # tauri_build::build()
  capabilities/default.json# shell:allow-spawn 放行 binaries/htd-backend
  .gitignore               # 忽略 /target /binaries /gen/schemas
  src/main.rs              # 拉起 sidecar、读 HTD_READY、navigate+show、退出时 kill
  icons/                   # 已生成：icon.svg 主源 + 32x32/128x128/128x128@2x PNG + icon.ico + icon.icns（不依赖 Rust）
  binaries/                # 构建产物，由 prepare-tauri-sidecar.mjs 生成，不入 Git
    htd-backend-<target-triple>[.exe]
```

**关键决策：webview 走 `http://127.0.0.1:<port>`，不用 Tauri 的 `asset://`。**
这样页面的 Origin 与现有 Web 版完全一致，`originGuard` 白名单无需任何改动，前端代码零适配——这正是「业务代码 0 改动」的前提。代价是必须等 sidecar 就绪才能 navigate，故窗口初始 `url: "about:blank"` + `visible: false`，收到 `HTD_READY` 后再 `navigate()` + `show()`。

> ✅ `src/main.rs` 已完成**编译前静态校验**（Rust 工具链已装，但未跑真机 `cargo build`）：
> - `navigate` 改传 `tauri::Url`（Tauri v2 接收 `Url` 非 `String`）
> - `capabilities/default.json` 权限 `shell:allow-spawn` 已对照 `tauri-plugin-shell` v2 源码核验存在
> - `tauri.conf.json` 补 `devUrl`/`frontendDist`（v2 `tauri_build::build()` 编译期必填）
> - `Cargo.toml` 依赖（tauri 2.11.5 / shell 2.3.6 / single-instance 2.4.4 / serde / serde_json）全部声明，无漏依赖
> - 占位 sidecar `binaries/htd-backend-x86_64-pc-windows-gnu.exe`（合法 PE）已就位，externalBin 嵌入可过
> 真机 `cargo build` 仍可能暴露细微 API 签名差异，按编译器提示修一行即可，逻辑无需变动（见 §3.6）。

### 3.3 复用现有资产，不做重复建设

后端 `package.json` 已有 `build:win` / `build:mac`（`@yao-pkg/pkg` → 单文件 exe），且 `pkg.assets` 已含 `../frontend/dist/**/*`。**sidecar 直接复用该 exe**，不重新发明打包链路。

新增 `backend/scripts/prepare-tauri-sidecar.mjs` 负责把 pkg 产物转成 externalBin 要求的命名
（`htd-backend-<target-triple>[.exe]`，triple 优先取 `rustc --print host-tuple`，无 Rust 时回落到平台默认值并告警），顺带解决了 pkg 产物文件名含中文的问题。已挂到 tauri.conf.json 的 `beforeDevCommand` / `beforeBuildCommand`。

### 3.4 main.rs 关键流程

1. `setup` 中取 `app_data_dir` 作为 `HTD_DATA_ROOT`，注入 `HTD_DESKTOP=1` 与起始端口 `HTD_PORT=17388`
2. `app.shell().sidecar("htd-backend")` 拉起（**只拉一次**：后端会自行在 17388/17389/17390 中顺延，并在 `HTD_READY` 回报真实端口，壳不需要自己重试）
3. 监听 `CommandEvent::Stdout`，匹配 `HTD_READY ` 前缀 → 解析 `url` → `navigate()` + `show()` + `set_focus()`
4. `RunEvent::ExitRequested | Exit` 时 `child.kill()`，确保不留孤儿 Node 进程（SQLite 单写锁）
5. single-instance 插件保证唯一实例；二次启动时聚焦已有窗口
6. sidecar 拉起失败时显示窗口并注入可读错误文案，避免用户面对无响应窗口

### 3.5 跨语言契约门禁（已接入 lint）

壳（Rust）与后端（Node）之间靠三样东西对齐，任一侧改动而另一侧没跟上都要到打包后才暴露：
① 就绪行前缀 `HTD_READY ` ② 起始端口 `17388` ③ sidecar 名称 `htd-backend`。

`backend/scripts/verify-tauri-config.js` 把这些契约固化成 **27 项静态断言**并接入 `npm run lint`：
骨架文件齐备、`READY_PREFIX` 与 `server.js` 输出字面量一致且以空格结尾、`START_PORT` 等于 `port.js` 的 `FIXED_PORTS` 首项且属于固定集合、`externalBin`/`capabilities`/`prepare-tauri-sidecar.mjs` 三处 sidecar 名一致、`main.rs` 注入三个 env、退出路径含 `kill()`、`Cargo.toml` 无 `[lib]` 段（缺 `lib.rs` 会导致编译失败）。

### 3.6 编译验证准备与真机 `cargo build`（持续进行）

为把「首次编译」从一堆零散手动步骤收敛成**一键**，并完成跨环境可验证，本回合补齐以下准备（均不依赖真机编译）：

1. **MinGW-w64 下载（进行中）**：winlibs GCC16/MSVCRT 7z（~105MB）落盘 `$USERPROFILE/.workbuddy/mingw64.7z`，提供 GNU target 编译所需的 `windres`/`gcc`/`ar`/`ld`。本机到 GitHub 链路偏慢且不稳（~30KB/s，常在 ~70MB 处断流），普通 `curl` 反复失败；已改用 `src-tauri/fetch-mingw.sh` 的**断点续传循环**（`curl -C -` + 外圈重启 + py7zr 完整性校验 + 自动解压），后台运行，断流即累计续传。
2. **crates.io 国内镜像**：`~/.cargo/config.toml` 指向 `rsproxy.cn` sparse 索引，Tauri 全家桶依赖下载走国内加速（直连仅 ~28KB/s）。
3. **`prepare-tauri-sidecar.mjs` 容错 + 前端自举**：默认（非 `--strict`）在 pkg 源 exe 缺失时保留已就位的 sidecar 并告警、`exit 0`，使 `beforeBuildCommand` 不中断；新增 `ensureFrontendDist()`——`frontend/dist/index.html` 缺失时自动跑 `vite build`，保证 `tauri_build` 编译期嵌入 `frontendDist` 不报错。
4. **`tauri.conf.json` 修正**：`frontendDist` 由错误的 `"../frontend"` 改为 `"../frontend/dist"`（`tauri_build` 编译期嵌入目录，必须指向构建产物）。`externalBin: ["binaries/htd-backend"]` 经核对**本就正确**——Tauri v2 会自动给 externalBin 追加 `-$TARGET_TRIPLE` 后缀，故无需 `{target}` 占位；本地 GNU 需 `htd-backend-x86_64-pc-windows-gnu.exe`（已就位），CI MSVC 需 `htd-backend-x86_64-pc-windows-msvc.exe`（由 prepare 脚本按 `rustc --print host-tuple` 生成）。
5. **GNU 链路配置 `src-tauri/.cargo/config.toml`**：显式指定 `[target.x86_64-pc-windows-gnu] linker = "x86_64-w64-mingw32-gcc"`、`ar = "x86_64-w64-mingw32-gcc-ar"`，避免 cargo 在 gnu target 找不到 cc；仅对 gnu 生效，CI（MSVC）忽略，无副作用。
6. **一键编译脚本 `src-tauri/build-tauri.sh`**：封装「解压 MinGW（py7zr）→ 注入 PATH → `cargo build`」，首次编译验证直接 `bash src-tauri/build-tauri.sh` 即可。占位 sidecar 已就位，故无需先 pkg 打包。
7. **GitHub Actions CI 工作流 `.github/workflows/build-tauri.yml`（新增，推荐验证路径）**：在 `windows-latest`（预装 VS Build Tools + Windows SDK）用 **MSVC target** 跑 `tauri build`，既真机编译验证 Rust 骨架、又产出 NSIS 安装包。`windows-latest` 自带 MSVC 工具链，故**完全绕开本地 105MB MinGW 下载的网络瓶颈**，是比本机 GNU 编译更稳的验证通道；产物（installer）作为 artifact 上传。

**真机编译验证：✅ 已完成（2026-09-12，走 CI/MSVC 通道）**

- `fetch-mingw.sh` 续传循环 40 次均为 0 字节——沙箱对 GitHub release 域名已限流，**本机 GNU 编译通道放弃**，不再投入时间。
- 改为 CI（`windows-latest` 预装 MSVC + Windows SDK）真机验证：**run `34704536841` 全绿**，其中 `Tauri build (cargo compile + nsis bundle)` 步骤 success，产出 NSIS 安装包 artifact `htd-workbench-installer`（**96,145,407 bytes**）。
- 结论：**S3-1 骨架编译 + 打包验证通过**。`main.rs` 无需修正（此前预修的 `navigate → tauri::Url` 判断正确）。
- 后续 S3-2/S3-3 的编译验证一律沿用此通道：`git push` → CI 约 8 分钟出结论，本机不再依赖 Rust 工具链。

---

## 4. S3-2 · 系统集成（**已实现**，2026-09-12；编译验证见 CI）

| 能力 | 方案 | 状态 |
|---|---|---|
| 系统托盘 | `TrayIconBuilder` + 原生菜单（显示/隐藏 · 打开数据目录 · 退出） | ✅ 已实现 |
| 关闭行为 | `WindowEvent::CloseRequested` → `prevent_close()` + `hide()`，**点 X 隐藏到托盘而非退出** | ✅ 已实现 |
| 全局快捷键 | `tauri-plugin-global-shortcut`：`Ctrl+Alt+H` 唤起/隐藏，仅 `ShortcutState::Pressed` 响应 | ✅ 已实现 |
| 原生通知 | `tauri-plugin-notification`：首次隐藏到托盘时提示一次（避免用户找不到窗口） | ✅ 已实现 |
| 单实例 | `tauri-plugin-single-instance`：二次启动聚焦已有窗口 | ✅ S3-1 已有 |
| 自动更新 | `plugin-updater` | ⏸ 暂缓：需签名密钥；且离线优先原则下默认不自动下载 |

### 4.1 关键设计决策

1. **点 X = 隐藏到托盘**：工作台是「随叫随到」的工具型应用，退出即重载后端（拉起 Node + 打开 SQLite）成本过高。用受管状态 `Quitting(AtomicBool)` 区分「真正退出」（托盘菜单退出）与「隐藏」，只有前者放行关闭。
2. **托盘图标复用打包图标**：`app.default_window_icon().cloned()`，不额外引入图片解码依赖（`Image` 已 `derive(Clone)`）。
3. **快捷键字符串而非枚举**：`global_hotkey` 的 `ShortcutWrapper` 只实现 `TryFrom<&str>`，故用 `"Ctrl+Alt+H"` 字符串注册。该解析器要求**主键必须是最后一个 token**（`"Ctrl+H+Alt"` 非法），写反了编译能过、运行时静默失效——已在门禁里静态校验格式。
4. **通知只弹一次**：`TrayHinted(AtomicBool)` 原子标记，避免每次隐藏都打扰。
5. **API 全部照源码核对**：本机 `cargo fetch` 已把 tauri 2.11.5 / 插件源码落到 `~/.cargo/registry`，`TrayIconBuilder`、`MenuBuilder`、`MenuItemBuilder`、`on_window_event`、`NotificationExt`、`GlobalShortcutExt` 等签名均逐项核对后编码，避免「编译 8 分钟一轮」的试错。

### 4.2 门禁扩项（27 → 46）

`verify-tauri-config.js` 新增 19 项 S3-2 契约断言：托盘构建、菜单事件绑定、`prevent_close`、三个菜单 id 常量被分支消费、快捷键常量与**格式合法性**、快捷键注册、通知调用、Cargo 依赖与 Rust `use` 双侧对应、`capabilities` 的 `notification:default` / `shell:allow-open`。

---

## 5. S3-3 · 视觉衔接

- ✅ 图标双形态：SVG（源码）+ PNG/ICO/ICNS（Tauri 打包用），清单与生成方式见 §3.6
- 窗口尺寸/最小尺寸持久化，与前端 `localStorage` 的外观设置不冲突（外观仍由服务端 SystemSetting 持久化）
- 启动闪屏：webview 加载前显示应用图标，避免白屏窗口闪现

---

## 6. 风险与对策

| 风险 | 对策 |
|---|---|
| Rust 工具链安装体积/耗时 | 单独维护窗口执行；未安装前保持 S3-1 不开工 |
| sidecar 孤儿进程导致 SQLite 锁 | main.rs 退出钩子 kill；再加退出前 `POST /system/shutdown`（如需要） |
| 固定三端口全被占用 | 壳侧提示用户释放；不改为随机端口（见 §2.4） |
| WebView2 缺失（Win10） | 安装包引导下载或内置引导安装 |
| 自动更新违背离线优先 | 默认仅提示不自动下载（§4） |

---

## 7. 待决策

1. ~~**何时安装 Rust 工具链**（GB 级下载 + 首次编译 30–60 分钟）~~ → **已完成 2026-09-12**：rustc 1.98.1 (x86_64-pc-windows-gnu) 已装，MinGW-w64 已下载，镜像/容错脚本/一键编译脚本就位；真机 `cargo build` 待 shell/网络恢复后执行（见 §3.6）
2. **V2-3 主密码 + AES-256-GCM 加密（已启动，2026-09-12）**：加密模块 `backend/src/lib/crypto.js` + 预览脚本 `backend/scripts/encrypt-db.mjs` + 设计文档 `docs/产品迭代/V2-3-加密方案.md` 已落地。零依赖（Node 内置 scrypt + AES-256-GCM），已在真实活跃库验证往返。剩余：服务端「启动解密/关闭加密」接入，待用户确认 UX 后实施（见 V2-3 方案 §5）。
3. 代码签名证书：自动更新需要，个人使用可暂缓。
