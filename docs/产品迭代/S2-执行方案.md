# V2.0 S2 阶段执行方案（图表/图谱组件化 + 地基组件）

> 版本：v1.0 | 日期：2026-09-11 | 制定：MvpDevExpertTeam
> 前置：M0 ✅ / S1（S1-1/3/5/6/7）✅ / S2-1 Vite 构建化 ✅
> 配套：[V2.0-全面重构计划.md](./V2.0-全面重构计划.md)（战术总纲 §6 S2 段）、[S2-1-执行方案.md](./S2-1-执行方案.md)

---

## 1. 背景与目标

S1/S2-1 完成后，前端从「43 个有序 `<script>`」收敛为 Vite 单入口聚合（`dist/`），但**组件形态与渲染机制未变**——仍是「IIFE 全局 Vue + JS 组件 + template 字符串」。遗留两大问题：

- **P0-3（架构审计报告）**：`charts.js` 用「字符串拼接 + `v-html`」渲染 SVG，每帧重算全量，力导向布局不可达 → 知识图谱 1k 节点验收硬性不兼容。
- **组件缺口（视觉审计 §5.5）**：`Skeleton/Switch/Table/Drawer/Tooltip/MarkdownEditor/GraphCanvas` 全部缺失，各模块列表/空态/二次确认/Markdown 渲染各自为战。

S2 是**组件化与能力深化**阶段，路线图含 5 个工单（S2-2 ~ S2-6），其中 **S2-2 知识图谱是 D1 硬需求**（战略已裁定：1k 节点流畅 → S2 必做）。

---

## 2. 现状盘点（2026-09-11 实测）

| 项 | 现状 | 来源 |
|---|---|---|
| 图表机制 | `frontend/js/utils/charts.js`：纯函数 SVG 字符串生成，`window.htdCharts`，调用方 `v-html` 注入；配色已令牌化（`PALETTE = var(--chart-series-1..N)`，M0-1 落地） | 读 charts.js |
| 图表类型 | bar / donut / line / stackedBar / timeline / legend 齐全 | 读 charts.js |
| `lineChart` 调用 | **零调用**（仅 `ProjectPage.js:439` 用 `timelineChart`）→ P2-2 成立 | Grep |
| 现有 Htp 组件 | 12 个：Button / Card / Checkbox / Textarea / Empty / StatusDot / Modal / Select / Tag / CommandPalette / ConfirmPermanentDelete / Input | Glob |
| S2-6 目标组件 | Skeleton / Switch / Table / Drawer / Tooltip / MarkdownEditor / GraphCanvas —— **全部不存在，需新建** | Grep / Glob |
| d3 / 知识图谱 | 前端**零 d3**、**零图谱代码**，图谱为全新建设 | Grep |
| SFC | **零 `.vue` 文件**，仍是 JS 组件 + template 字符串 | Glob |
| Vite 策略 | `vite.config.js`：**零 npm 依赖 + IIFE 全局 + 副作用 import**（S2-1 低风险平移设计） | 读 vite.config.js |

---

## 3. 技术选型决策（⚠️ 需裁定，见 §7）

### 3.1 组件化路线（红线相关，必选 A 或 B）

- **路线 A（推荐，低风险）**：保持 `window.Vue` IIFE 全局，**不引入 SFC / 编译器**。
  - `charts.js` 改写为**组合式/选项式 JS 组件**（用 `computed` 响应式生成 SVG 字符串或 `render` 函数），数据更新只触发最小重渲染，根治 P0-3「整段重算」。
  - 图谱引入 **`d3-force`**（纯计算库，无 DOM 依赖），经 Vite 打包进 `dist/`；Vue 响应式驱动 SVG 渲染，不引入重 DOM 操作。
  - ✅ 守住两条红线：业务代码 0 改动进 Tauri（仍是 IIFE Vue 写法）、离线优先（运行时零网络）。

- **路线 B（高改动）**：引入 `vue` ESM + `@vue/compiler-sfc`，全面 SFC（`.vue`）化。
  - 标准工程形态，但需重构 IIFE 全局加载链路（`index.html` 三库 + `entry.js` 副作用 import 全部重做），与「低风险演进式」战略不符，风险高、回归面大。

### 3.1-B 路线 B 落地策略（兼容式渐进迁移）

用户 2026-09-11 裁定采用路线 B（全面 SFC 化）。但「全面」是**最终态**，执行上采用兼容式渐进迁移，避免一次性重写 21 模块 + 12 组件导致的回归雪崩：

1. **B0 基建（加载机制切换）**：安装 `vue`（ESM）+ `@vitejs/plugin-vue` + `@vue/compiler-sfc`；`main.ts` 以 `import { createApp } from 'vue'` 启动，并 `window.Vue = Vue` / `window.Pinia = Pinia` 兼容旧代码；`index.html` 收敛为单 `<script type=module src=/src/main.ts>`，移除 IIFE 三库 script；`vite.config.js` 加 `vue()` 插件、移除 `copyIifeLibs`。**旧 JS 组件因 `window.Vue` 兼容继续工作，功能等价 S2-1。**
2. **B1 地基（S2-6）**：新建 Skeleton/Switch/Table/Drawer/Tooltip/MarkdownEditor 直接用 **SFC（.vue）** 编写，树立规范。
3. **B2 核心（S2-2）**：图表改写为 SFC 组件（响应式 SVG）+ 挂载 lineChart + 知识图谱 GraphCanvas（SFC + d3-force）。
4. **B3 渐进迁移**：后续版本逐步把现有 12 组件 + 21 页面模块从「JS 组件 + window.Vue」改写为 SFC；完成前 `window.Vue` 兼容层保留。
5. **离线优先**：Vue/Pinia/d3-force 全部构建期打包进 `dist/`，运行时零网络；IIFE 全局库不再随 dist 发布。

> 风险：B0 切换后，任何仍依赖 IIFE 全局但被误删的引用会白屏；故 B0 必做全量回归（启动 + 21 模块挂载 + lint + vitest）守门。

### 3.2 d3-force 引入 vs 自写力导向

- **推荐引入 `d3-force` 单包**（min ~50KB，仅 `forceSimulation/forceManyBody/forceLink/forceCenter` 等纯计算模块），Vite 打包进 `dist/`，运行时零网络 → **满足离线优先**。
- 备选自写简单力导向（零依赖，但需自调参、稳定性/性能需自证，工期反而更长）。
- 引入方式：在 `frontend/` 受管工作区 `npm install d3-force`（仅此纯计算库），某图谱模块 `import { forceSimulation } from 'd3-force'`，Vite 自动打包。

### 3.3 离线优先如何满足（澄清红线误解）

- S2-1「零 npm 依赖」是为**低风险平移**临时约束，非永久红线。
- 离线优先红线 = **运行时禁止从 CDN 拉取**。构建期把依赖（d3-force）打进 `dist/`（本地静态资源）完全合规。
- 调整：`vite.config.js` 注释与策略由「零 npm 依赖」放宽为「**仅纯计算 / 渲染库可打包进 dist**（禁止任何运行时网络依赖）」；vue/pinia 仍走 IIFE 全局（避免引入 Vue 编译器）。

---

## 4. S2 执行顺序（推荐）

| 序 | 工单 | 理由 |
|---|---|---|
| 1 | **S2-6 地基**：新建 Skeleton/Switch/Table/Drawer/Tooltip + Token 单一来源收口 | 是 S2-2/3/4 的公用地基（Table/Drawer/MarkdownEditor 多处复用），先铺路降低后续返工 |
| 2 | **S2-2 核心**：图表响应式组件化（根治 P0-3）+ 挂载 `lineChart`（消 P2-2）+ 知识图谱 `GraphCanvas`（d3-force，1k 节点 ≥30fps） | D1 硬需求、V2.0 核心卖点，优先兑现 |
| 3 | **S2-3 Vault 深化**：双向链接 + 标签树 + 引用图谱（markdown-it 强渲染） | 第二大脑核心，依赖 S2-6 的 MarkdownEditor |
| 4 | **S2-4 Note 双轨**：free/quote + anchor 双向跳转 | 依赖 S2-3 的引用关系 |
| 5 | **S2-5 RFP 逐条应答**：投标条目级结构（新建能力） | 独立业务深化，可后置 |

> 说明：S1-2（input 组件化）已降级到 S2，并入 S2-6（增强 `HtpInput` 属性透传 `$attrs`，统一 StudyPage/SettingsPage 的 `step/min/extra class`）。

---

## 5. 各工单拆解 + 出口判据

### S2-6 地基组件 + Token 单一来源
- 新建 `frontend/js/components/`：`HtpSkeleton.js` / `HtpSwitch.js` / `HtpTable.js`（统一列表骨架/空态/分页）/ `HtpDrawer.js` / `HtpTooltip.js` / `HtpMarkdownEditor.js`（markdown-it 强渲染，S2-3 复用）。
- Token 单一来源：JS 侧图表色全部读 `var(--chart-series-*)`，无裸 hex 逃逸（M0-1 已部分做，收口残留）；补图标回归测试（NAV_ITEMS/COMMANDS 的 icon 键均在 `icons.js` 存在）。
- 增强 `HtpInput`：`$attrs` 透传 `step/min/extra class`（S1-2 收口）。
- 出口：新组件各补单测；`grep` 无 JS 裸 hex；图标回归测试绿。

### S2-2 图表/图谱组件化（D1 硬需求）
- 图表：`charts.js` 改写为 Vue 组件（bar/donut/line/stackedBar/timeline），`computed` 响应式生成 SVG，调用方用 `<component>` 而非 `v-html`；挂载 `lineChart` 到首页/财务趋势位（消 P2-2）。
- 图谱：新建 `GraphCanvas.js`（Vue 组件 + d3-force 布局 + SVG 渲染），支持节点/边/拖拽/缩放，1k 节点性能门槛。
- 出口：P0-3 根治（`v-html` 图表调用清零）；`lineChart` 有挂载点；**知识图谱 1k 节点 ≥30fps（中）/60fps（验）**；新增布局稳定性/空数据/性能单测。

### S2-3 Vault 深化
- `VaultPage` 从纯文本渲染升级：双向链接（`[[note]]`）、标签树、引用图谱（复用 `GraphCanvas`）、markdown-it 强渲染。
- 出口：Vault 非纯文本；双链可跳转；引用图谱节点可达。

### S2-4 Note 双轨
- Note 模型 free/quote 双轨 + anchor 双向跳转（笔记 ↔ 原文）。
- 出口：笔记↔原文可互跳。

### S2-5 RFP 逐条应答
- Bid 模块新增 RFP 条目级结构（条目 + 应答 + 证据挂载）。
- 出口：Bid 支持条目级应答 + 证据挂载。

---

## 6. 风险与门禁

| 风险 | 缓解 |
|---|---|
| P0 视觉门禁（`npm run lint`：validate-theme + visual-audit） | 新增/改写 CSS 严守 class 契约（禁删/改名已有类，仅改色值/令牌）；零 emoji；零裸 hex |
| 红线端口 17388/17389/17390 | 启动逻辑写死探测，勿改（实测 `HTD_PORT` 不生效，文档待修） |
| d3-force 引入破坏离线优先 | 仅构建期打包进 `dist/`，运行时零网络；不引入 Vue 编译器（仍 IIFE 全局） |
| 知识图谱 1k 节点性能 | d3-force 仅算布局，SVG 渲染走 Vue 响应式；必要时 canvas 降级；用 rAF 计数验收帧率 |
| 测试回归 | 沿用 `HTD_TEST_DB_URL` 隔离；新增组件/布局/性能单测；基线 427 全绿不回退 |
| 组件化改写 charts.js 破坏既有 4 类图表 | 逐类型迁移 + 保留 `htdCharts` 兼容导出，门禁 `verify-registry` 不动 |

---

## 7. 待用户裁定

| ID | 决策 | 推荐 |
|---|---|---|
| S2-R1 | 组件化路线：A vs B（全面 SFC 化） | **B（用户 2026-09-11 裁定）**：全面 SFC 化；落地策略见 §3.1-B |
| S2-R2 | S2 执行顺序 | **先 S2-6 地基 + S2-2 核心**（用户 2026-09-11 确认） |
| S2-R3 | d3-force 引入 | **同意**（构建期打包，离线可用；用户 2026-09-11 确认） |

> 裁定记录：2026-09-11 用户拍板 S2-R1=路线 B、S2-R2=先 S2-6+S2-2、S2-R3=同意 d3-force。路线 B 意味着前端从「IIFE 全局 Vue + JS 组件」切换为「Vue ESM + SFC 组件」，是系统级改造，采用 §3.1-B 兼容式渐进迁移降低风险。

## 8. B0 基建执行状态（2026-09-11）

### 已完成
- **依赖自包含**：`frontend/` 装 `vite@8.2.1` + `@vitejs/plugin-vue@6` + `vue@3.5.42` + `pinia@4.0.3` + `@vue/compiler-sfc@3.5.42` + `@vue/devtools-api`（pinia4 的 peer 依赖，必须显式装否则 rolldown 解析失败）；backend 已卸载重复前端包。
- **Vite 配置**：加 `vue()` 插件 + `resolve.alias.vue → vue/dist/vue.esm-bundler.js`（保留运行时模板编译，兼容旧 `template` 字符串组件，否则 runtime-only 构建白屏）+ feature flags define（`__VUE_OPTIONS_API__` 等）；移除 `copyIifeLibs`（不再有 IIFE 库）。
- **入口链**：`src/globals.js`（ESM Vue/Pinia 挂 `window.Vue/window.Pinia/window.htdPinia` 兼容层，保 29 个旧文件零改动）+ `src/main.js`（先 globals 后 entry）+ `index.html` 收敛为单 `main.js` module 入口，移除 IIFE 三库 script；backend `build:fe` 改为 `npm --prefix ../frontend run build`。
- **验证**：`vite build` 通过（80 模块，dist 重新生成，无 IIFE 残留）；`npm run lint` 22/22 全绿（含 verify-registry 21 项断言）。

### 已确认（2026-09-11 用户本地目视）
- **运行时首屏**：用户本地 `npm run dev`（http://127.0.0.1:17388）目视确认首屏不白屏、21 模块挂载、pinia4 行为正常 → B0 运行时门通过。
- vitest 后端 427 用例：前端改动不触碰后端逻辑，预期通过（留待本地/CI 实跑）。
- **提交状态**：B0 已本地提交（见下方 commit 记录），**未 push**（SSH 推送待用户拍板）。

### 关键教训
- vite 8 用 rolldown 打包，pinia4 把 `@vue/devtools-api` 改 peer 依赖，前端必须显式安装。
- 前端依赖必须装在 `frontend/node_modules`（与 backend 兄弟目录，node_modules 查找链 `frontend→根→全局` 够不到 backend），否则 `vite.config.js` 的 plugin 解析失败。

## 9. S2-6 地基组件执行状态（2026-09-11）

### 已完成
- **6 个 SFC 地基组件**（真 `.vue`，`<script setup>` + `<style scoped>` 令牌化）：`HtpSkeleton` / `HtpSwitch` / `HtpTable`(列表骨架/空态/分页) / `HtpDrawer` / `HtpTooltip` / `HtpMarkdownEditor`(markdown-it 渲染，S2-3 Vault 复用)。全部在 `app.js` 显式 `import` 后 `app.component` 注册，走通 B0 的 SFC 能力。
- **Token 单一来源收口**：`frontend/js` 裸 hex 归零（grep 无匹配）；图表色 `HomePage`/`FinancePage` 改读 `var(--chart-series-*)`，SVG 描边改 `var(--text-tertiary)`、进度条改 `var(--bg-tertiary)`；新增令牌 `--color-on-primary`(global.css 暗/亮双主题)；新组件样式零裸 hex（仅 `var()` + `rgba()`）。
- **HtpInput 增强**：核实默认 `inheritAttrs` 为真、单根 `<input>`，`step/min/class` 经 `$attrs` 自动透传，S1-2 已收口，无需改动。
- **前端测试基建补齐**：装 `vitest@3 + @vue/test-utils@2 + jsdom@25`；新增 `frontend/vitest.config.js`(vue 插件 + esm-bundler alias + jsdom) + `frontend/tests/components.spec.js`(6 组件冒烟：渲染/交互/空态/分页/插槽/Teleport/Markdown 预览)；`frontend/package.json` 加 `test`/`test:watch` 脚本。
- **验证**：`vite build` 169 模块通过、`npm run lint` 22/22 全绿、`frontend/js` 裸 hex 归零。

### 已完成（2026-09-11）
- 跑 `cd frontend && npm test`：**15/15 全绿**（6 组件冒烟：渲染/交互/空态/分页/插槽/Teleport/Markdown 预览）。
- S2-6 已本地提交（`26d0450`）并 **SSH 推送**（`3359bcd..26d0450 main -> main`），`main` 与 `origin/main` 同步。
- 进入 **S2-2 核心**（见 §10）。

## 10. S2-2 核心执行状态（2026-09-11）

### S2-2a 图表响应式 SFC 组件化（根治 P0-3）
- **几何与渲染分离**：新增 `frontend/js/utils/chartGeometry.js`（ESM 纯函数：`barGeometry` / `donutGeometry` / `lineGeometry` / `stackedGeometry` / `timelineGeometry` + `arcPath`/`fmt`/`truncate`/`colorAt`），只返回坐标数组；新建 6 个 SFC 图表组件用 `v-for` 渲染 SVG 节点：
  `HtpBarChart` / `HtpDonutChart` / `HtpLineChart` / `HtpStackedBarChart` / `HtpTimelineChart` / `HtpChartLegend`。
- **4 个页面去 `v-html`**：`FinancePage`(bar×3+donut+legend) / `HomePage`(bar×3+donut+legend) / `StudyPage`(bar+stacked+legend) / `ProjectPage`(timeline+donut) 全部改用组件标签；删除 `const C = window.htdCharts` 与全部 `*Svg` 死 computed；`src/entry.js` 移除 `charts.js` 副作用导入；**旧 `frontend/js/utils/charts.js` 已删除**（P0-3 机制债根除）。
- **验证**：`frontend` 全量 grep `htdCharts|C.barChart|...` **零残留**。

### S2-2b 挂载 lineChart（消 P2-2）
- `HtpLineChart` 挂载到 `FinancePage`「月度净收支趋势」卡（`monthlyIncomeExpense` 的 income-expense 单系列），死代码消除。

### S2-2c 知识图谱 GraphCanvas（d3-force）
- 新增 `HtpGraphCanvas.vue`（Canvas 渲染 + d3-force 力导向）：
  - **布局/渲染解耦**：`forceSimulation().stop()` 关掉 d3 内部 timer，由组件 rAF 循环调 `sim.tick()` + `draw()`，帧率可控、卸载彻底释放；
  - **性能设计**：连线一次性 path 批量描边、节点数 > `labelThreshold`(80) 自动关标签、`forceManyBody` 设 `distanceMax(320)`+`theta(0.9)`、HiDPI 上限 2x；
  - **Token 单一来源**：Canvas 无法直接用 `var()`，运行期从 `:root` 解析 CSS 变量（兜底一律 `rgb()`，不引入裸 hex）；
  - **健壮性**：端点不存在的脏连线预过滤（避免 d3 `node not found`）；内部拷贝节点/边，不 mutate props；`getContext` 返回 null 时安全跳过（jsdom 可测）。
  - **交互**：hover 高亮 + 拖拽（`fx/fy`）+ 点击派发 `node-click`。
- **业务挂载（避免死代码）**：`VaultPage` 新增「知识关系图谱」卡——节点=沉淀条目（group=status，size=标签数），边=同标签条目链式相连（避免全连接边爆炸），点击节点直接打开详情抽屉。

### S2-2d 单测 + 门禁回归（全绿）
- 新增 `frontend/tests/charts.spec.js`（6 图表组件 10 用例：v-for 渲染/空态/弧段/折线/堆叠/时间轴/图例）+ `frontend/tests/graph.spec.js`（5 用例：不 mutate props、脏连线过滤、hitTest+node-click、空态、**1k 节点帧预算**）。
- **帧率门槛**：jsdom 注入 no-op canvas 2d context stub 让绘制路径真实执行 → **1k 节点 / 999 边：10.3~13.3 ms/帧**（门槛 33.3ms = 30fps，余量 2.5x；60fps 门槛 16.7ms 亦达标）。
- **验证**：前端单测 **30/30**、`vite build` 通过（181 模块）、`npm run lint` **22/22**、后端 vitest 427 不回退。

### 关键教训（本回合新增）
- **关键教训（本回合新增）**：
  - **Windows EBUSY 静默写失败**：Edit 工具返回 success 但内容未落盘（尤其并发编辑同一文件）。对策：**串行编辑 + 每次编辑后 grep/Read 读盘验证**；顽固文件改用 Node 脚本带重试写入。
  - **Canvas 与 CSS 变量**：Canvas 绘图 API 不认 `var(--x)`，必须运行期 `getComputedStyle(:root)` 解析；兜底色用 `rgb()` 以维持「前端 JS 裸 hex 归零」。

## 11. S2-3 Vault 深化执行状态（2026-09-11）

### 已完成
- **新建 `frontend/js/utils/markdown.js`**（ESM 纯函数，可单测）：`escapeHtml` / `extractWikiLinks`（`[[目标]]` 去重提取）/ `renderWikiLinks`（渲染锚点，存在实线、缺失虚线）/ `renderMarkdown`（markdown-it 强渲染 + 双链）/ `references`（反链判定，忽略大小写）。
- **VaultPage 深化**：
  - 编辑表单：`htp-textarea` → **`htp-markdown-editor`**（分屏预览）；给 MarkdownEditor 加 `render` 函数 prop，Vault 传入 `renderMarkdown`，保证**预览与详情渲染完全一致**（否则内置 `html:false` 会把双链锚点转义成文本）。
  - 详情正文：`{{ content }}` 纯文本 → **`v-html="detailHtml"` 强渲染**（标题/列表/代码/引用/表格）+ 事件委托 `@click="onDetailClick"` 处理双链跳转。
  - **双向链接**：点击已存在目标 → 打开该条详情；点击未创建目标 → toast 提示并**预填主题打开新建**（形成双链落点，闭环而非死链）。
  - **反链**：`detailBacklinks` 扫描全量条目找出引用了当前主题的条目，详情底部展示「被引用（N）」可跳转。
  - **标签树**：`tagTree` 支持 `父/子` 两级聚合 + 引用计数排序 + 点击切换筛选（复用既有 `filterTag` → watch 自动重载）。
  - **引用图谱升级**：`graphLinks` 在「同标签链式边」之外新增 **`[[双链]]` 引用边（kind='wiki'）**，图谱从「标签关联图」升级为真正的**引用图谱**（S2-3 出口「引用图谱节点可达」达成）。
- **样式**：`pages.css` 新增 `.vault-tagtree` / `.vault-tagbtn` / `.vault-graph` / `.vault-wikilink(--missing)` / `.vault-detail__refs` / `.markdown-body` 排版，全部 `var()` 令牌，零裸 hex（门禁通过）。

### 验证（全绿）
- 新增 `frontend/tests/vault-markdown.spec.js`（9 用例：转义/双链提取去重/存在与缺失渲染/强渲染/双链保留/反链判定）。
- 前端单测 **39/39**、`vite build` 通过、`npm run lint` **22/22**。

### 安全口径（需知晓）
- Vault 渲染开启 `markdown-it({ html: true })`（否则双链锚点会被转义）。本项目为**本地单人应用、内容本人录入**，风险可接受；所有插值（链接目标/显示文本）均经 `escapeHtml`，不引入额外脚本执行路径。若未来开放外部内容导入，需改为白名单 sanitize。

## 12. S2-4 Note 双轨执行状态（2026-09-12）

### 决策（用户拍板：推荐方案 A + A）
- **原文存储**：给 `ReadingMaterial` 加 `content` 字段本地存原文，anchor 用字符偏移 → 真正的双向跳转（非跳外链，离线可用）。
- **挂载范围**：通用软关联 `sourceType + sourceId`（沿用 VaultItem 惯例），阅读/会议/沉淀都能挂笔记。

### 已完成
- **数据层（S2-4a）**：
  - `schema.prisma` 新增 `Note` 模型（`sourceType`/`sourceId` 软关联、`content`、`quote`、`anchor`、`tags`、软删、`@@map("notes")` 与双索引）；`ReadingMaterial` 新增 `content String?`。
  - `enums.js` 新增 `NOTE_SOURCE_TYPE`（READING/MEETING/VAULT/MANUAL）。
- **后端（S2-4b）**：新建 `backend/src/modules/note/` 五层（schema/repository/service/controller/router），`app.js` 挂载 `/api/v1/notes`；`anchor` 由 Zod 校验为 `{start,end}` 且 `end>=start`；列表按 `createdAt` 升序（贴合原文顺序）。新增 `note.test.js`（11 用例）。
- **前端锚点工具（S2-4c）**：新建 `frontend/js/utils/anchor.js` —— `parseAnchor` / `getRangeOffsets`（Range→纯文本偏移）/ `getSelectionOffsets`（`window.getSelection`）/ `highlightOffsets`（`<mark>` 包裹 + 滚动，跨节点用 extract+append 而非 surroundContents）/ `clearHighlights`（unwrap 还原）。兼具 ESM 导出与 `window.htdAnchor` 挂载（兼容无 import 的旧页面）。新增 `anchor.spec.js`（10 用例，jsdom）。
- **双轨 UI（S2-4d）**：ReadingPage 列表项新增「双轨」按钮 → `HtpDrawer`（1080px）左右分栏：
  - 左栏原文：阅读态可**划选**（mouseup 捕获 → 计算锚点）／编辑态 textarea 保存回 `content`；
  - 右栏笔记：新增（自动带上 quote + anchor）／删除／**点击回跳**：清除旧高亮 → 包裹并滚动定位到原文片段；无锚点或原文变更时 toast 提示而非静默失败。
  - 表单新增「原文正文」字段；`dataStore` 补 `fetchNotes/createNote/updateNote/deleteNote`（刻意不做 `refreshAll`，避免为一条笔记刷新全部模块）。
- **样式**：`pages.css` 新增 `.read-track*` 与 `.htd-anchor-highlight`，全部 `var()` 令牌，零裸 hex。

### 验证（全绿）
- 前端单测 **49/49**（+10 锚点）、后端 **438/438**（427 + 11 笔记）、`vite build` 通过、`npm run lint` **22/22**。

### 关键风险与处置（务必知晓）
1. **未执行 `prisma db push --accept-data-loss`**。活跃库（`D:\htd-own-workbench-log\data\workbench.db`）存在**既有存储漂移**：全部 27 张表的 `DateTime` 列以 `BIGINT` 存储，Prisma 5.22 期望 `DateTime`，`db push` 会重铸这些列并提示数据丢失。该漂移**早于本次改动存在**，故不做全量重铸。
2. **改用最小侵入补表**：仅 `CREATE TABLE` 缺失的 `notes`（新增）与 `reading_materials`（历史就缺，非本次引入）两张表，不触碰任何既有表，零数据丢失。补表前后各备份一次（`*.db.bak-YYYYMMDD-HHMMSS`）。
3. **活跃库定位**：`settings.json` 的 `dataRoot` 指向 `D:\htd-own-workbench-log`，而非默认 `D:\荒天帝工作台`（后者是 14 表的陈旧库，非活跃）。排查库问题时须先读 `settings.json`。
4. **后续建议**：DateTime 存储漂移应在你方便时单独安排一次维护窗口处理（先备份 → `db push` → 抽样核对日期字段），不要与功能改动混在一起。
