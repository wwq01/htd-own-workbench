# Dependencies and local environments
node_modules/
backend/.env
*.local

# Build and runtime output
build/
backend/build/
frontend/dist/
coverage/
*.log
*.db-journal
*.db-wal
*.db-shm

# Local SQLite data; keep the empty schema template as a project asset.
*.db
!backend/prisma/template.db

# Operating-system and editor files
.DS_Store
Thumbs.db
.idea/
.vscode/

# WorkBuddy local memory / skills / artifacts (must never be committed)
.workbuddy/

# S4 服务端部署：运行日志与含访问令牌的本地配置
logs/
deploy/server.env

# 生成的审计报告（每次 lint 重写，仅含时间戳差异）
backend/scripts/visual-audit-report.json
